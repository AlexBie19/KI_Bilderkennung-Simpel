# Python dependencies for training the Fashion-MNIST clothing classifier.
# Install with:  pip install -r requirements.txt

# Deep learning framework and TFLite converter.
# TensorFlow 2.15+ includes the Fashion-MNIST dataset via keras.datasets
# and supports all augmentation layers used in the training pipeline.
tensorflow>=2.15.0,<3.0.0

# Numerical arrays (used for image data manipulation).
numpy>=1.24.0,<2.0.0
